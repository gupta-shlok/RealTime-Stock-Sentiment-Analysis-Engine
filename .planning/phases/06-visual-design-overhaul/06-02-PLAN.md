---
phase: 06-visual-design-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.js
  - frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.css
  - frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/SentimentHeatmap.css
  - frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/SentimentHeatmap.js
  - frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/SectorSentimentRow.css
  - frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/SectorSentimentRow.js
  - frontend/stock_sentiment_analysis/src/components/StockChart/StockChart.css
  - frontend/stock_sentiment_analysis/src/App.css
autonomous: true
requirements:
  - VIS-04
  - VIS-05
  - VIS-06
  - VIS-07
  - VIS-09
  - VIS-10

must_haves:
  truths:
    - "The TopBar shows 'StockSentimentSense' as a text logo — no image tag, no orange h1"
    - "The TopBar background is var(--bg-surface) with a 1px solid var(--border) bottom border"
    - "The Settings dialog has a theme toggle below the refresh interval selector"
    - "Sector skeleton loaders are visible on both dark and light backgrounds"
    - "Heatmap and sector cards use flat surface styling (no glassmorphism)"
    - "The ticker strip uses var(--bg-page) background and var(--border) top border"
    - "A .surface-card utility class exists in App.css replacing .glass-card globally"
  artifacts:
    - path: "frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.css"
      provides: "Restyled TopBar with surface background and border"
      contains: "var(--bg-surface)"
    - path: "frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.js"
      provides: "Text logo, theme toggle in Settings, all sx props use CSS variables"
      contains: "StockSentimentSense"
    - path: "frontend/stock_sentiment_analysis/src/App.css"
      provides: ".surface-card utility class"
      contains: ".surface-card"
  key_links:
    - from: "frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.js"
      to: "frontend/stock_sentiment_analysis/src/context/StockDataContext.js"
      via: "destructures theme and setTheme from useContext(StockDataContext)"
      pattern: "theme.*setTheme.*useContext"
    - from: "frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/SentimentHeatmap.js"
      to: "frontend/stock_sentiment_analysis/src/App.css"
      via: "className='surface-card' replaces className='glass-card'"
      pattern: "surface-card"
---

<objective>
Restyle the TopBar with the StockSentimentSense text logo, add the theme toggle to the Settings dialog, replace all hardcoded hex values in TopBar's sx props with CSS variables, and apply the CSS variable refactor to SentimentHeatmap, SectorSentimentRow, and StockChart. Replace .glass-card globally with the new .surface-card utility class.

Purpose: Phase 6's visual identity hinges on the TopBar — it's the first thing users see. The .glass-card migration is critical because it's used across multiple components; centralizing it in App.css prevents the divergence pitfall documented in research.

Output: TopBar showing text branding with theme toggle; all 4 CSS files using variable references; .surface-card replacing .glass-card in JSX and CSS; skeleton loaders using backgroundColor (not bgcolor) with var(--bg-elevated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-design-overhaul/06-CONTEXT.md
@.planning/phases/06-visual-design-overhaul/06-RESEARCH.md
@.planning/phases/06-visual-design-overhaul/06-01-SUMMARY.md
</context>

<interfaces>
<!-- From StockDataContext.js (created in Plan 01) — theme fields now available -->
```javascript
// Destructure in TopBar.js:
const { isRefreshing, lastUpdated, refreshInterval, setRefreshInterval, theme, setTheme } = useContext(StockDataContext);
```

<!-- CSS variables now available from theme.css (Plan 01) -->
```
--bg-page, --bg-surface, --bg-elevated, --text-primary, --text-secondary,
--text-disabled, --border, --accent-blue, --color-positive, --color-negative
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restyle TopBar — text logo, CSS variable sx props, theme toggle in Settings</name>
  <files>
    frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.js
    frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.css
  </files>
  <read_first>
    - frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.js (current content — must preserve all refresh/settings logic)
    - frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.css (current content — need to see h1 and #logo rules being removed)
  </read_first>
  <action>
**TopBar.js changes** (per D-01, D-02, D-04, D-22, D-23, D-24, VIS-04, VIS-05, VIS-07):

1. Add `LightModeIcon` and `DarkModeIcon` to MUI imports:
```javascript
import LightModeIcon from '@mui/icons-material/LightMode';
import DarkModeIcon from '@mui/icons-material/DarkMode';
import { Box, Typography } from '@mui/material';
```
(Add `Box` and `Typography` to the existing `@mui/material` import line if not present.)

2. Update the `useContext` destructure to include `theme` and `setTheme`:
```javascript
const { isRefreshing, lastUpdated, refreshInterval, setRefreshInterval, theme, setTheme } = useContext(StockDataContext);
```

3. Replace the `<a href="/"><img src="download.png" alt="Logo" id="logo" /></a>` with a text logo span (per D-01, D-02):
```jsx
<span className="topbar-brand">StockSentimentSense</span>
```

4. Update the `LinearProgress` sx prop: replace `'& .MuiLinearProgress-bar': { backgroundColor: '#3b82f6' }` with `'& .MuiLinearProgress-bar': { backgroundColor: 'var(--accent-blue)' }`. Keep `backgroundColor: 'rgba(59,130,246,0.2)'` unchanged (theme-stable).

5. Update the `SettingsIcon` `IconButton` sx prop: replace `sx={{ color: '#94a3b8', ml: 1 }}` with `sx={{ color: 'var(--text-secondary)', ml: 1 }}`.

6. Update the `Dialog` `PaperProps` (full replacement per research audit):
```jsx
PaperProps={{ sx: { backgroundColor: 'var(--bg-surface)', color: 'var(--text-primary)', minWidth: 320 } }}
```

7. Update `DialogTitle` sx prop:
```jsx
sx={{ color: 'var(--text-primary)', borderBottom: '1px solid var(--border)' }}
```

8. Update `InputLabel` sx prop:
```jsx
sx={{
    color: 'var(--text-secondary)',
    '&.Mui-focused': { color: 'var(--accent-blue)' },
    '&.MuiFormLabel-filled': { color: 'var(--text-secondary)' },
    backgroundColor: 'var(--bg-surface)',
    px: 0.5,
}}
```

9. Update `Select` sx prop:
```jsx
sx={{
    color: 'var(--text-primary)',
    '& .MuiOutlinedInput-notchedOutline': { borderColor: 'var(--text-disabled)' },
    '&:hover .MuiOutlinedInput-notchedOutline': { borderColor: 'var(--text-secondary)' },
    '&.Mui-focused .MuiOutlinedInput-notchedOutline': { borderColor: 'var(--accent-blue)' },
    '& .MuiSvgIcon-root': { color: 'var(--text-secondary)' },
}}
```

10. Update each `MenuItem` sx prop:
```jsx
sx={{ backgroundColor: 'var(--bg-surface)', color: 'var(--text-primary)', '&:hover': { backgroundColor: 'var(--bg-elevated)' }, '&.Mui-selected': { backgroundColor: '#1d4ed8' }, '&.Mui-selected:hover': { backgroundColor: '#2563eb' } }}
```
(Keep `#1d4ed8` and `#2563eb` — selected state blue, intentionally specific, not a semantic theme color.)

11. Update `DialogActions` sx prop:
```jsx
sx={{ borderTop: '1px solid var(--border)', px: 3, pb: 2 }}
```

12. Update Cancel `Button` sx prop: `sx={{ color: 'var(--text-secondary)' }}`

13. Update Save `Button` sx prop: `sx={{ backgroundColor: 'var(--accent-blue)' }}`

14. Add theme toggle block inside `<DialogContent>`, after the closing `</FormControl>` tag and before `</DialogContent>`:
```jsx
<Box sx={{ mt: 3, display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
    <Typography sx={{ color: 'var(--text-secondary)', fontSize: '0.875rem' }}>
        Theme
    </Typography>
    <IconButton
        onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}
        size="small"
        sx={{ color: 'var(--text-secondary)' }}
        aria-label="Toggle theme"
    >
        {theme === 'dark' ? <LightModeIcon fontSize="small" /> : <DarkModeIcon fontSize="small" />}
    </IconButton>
</Box>
```

**TopBar.css changes** (per D-22, D-23, D-24, VIS-05, research audit):

1. Change `.header` `background-color: rgb(0, 0, 0)` to `background-color: var(--bg-surface)`.
2. Add to `.header`: `border-bottom: 1px solid var(--border);`
3. Remove the `h1 { color: rgb(247,105,0); }` rule entirely.
4. Remove the `#logo { height: 90px; width: auto; }` rule entirely.
5. Remove the `.logo-container { padding-right: 5px; }` rule entirely.
6. Change `.custom-sentiment-button` `background-color: #f60` to `background-color: var(--accent-blue)`.
7. Change `.custom-sentiment-button:hover` `background-color: #e55` to `background-color: var(--accent-blue); filter: brightness(0.85);`
8. Change `.last-updated-text` `color: #94a3b8` to `color: var(--text-secondary)`.
9. Add `.topbar-brand` rule:
```css
.topbar-brand {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--accent-blue);
    letter-spacing: -0.02em;
    white-space: nowrap;
    padding: 0 16px;
}
```
  </action>
  <verify>
    <automated>grep -n "StockSentimentSense\|topbar-brand\|var(--bg-surface)\|setTheme\|LightModeIcon" frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.js frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.css</automated>
  </verify>
  <acceptance_criteria>
    - TopBar.js does NOT contain `download.png` or `id="logo"` or `&lt;img` referencing the logo
    - TopBar.js contains `&lt;span className="topbar-brand"&gt;StockSentimentSense&lt;/span&gt;`
    - TopBar.js does NOT contain any hardcoded hex values from this list: `#94a3b8`, `#1e293b`, `#f1f5f9`, `#3b82f6`, `#475569`, `#93c5fd`, `#334155`, `#cbd5e1`
    - TopBar.js contains `setTheme(theme === 'dark' ? 'light' : 'dark')`
    - TopBar.js contains `LightModeIcon` and `DarkModeIcon` imports
    - TopBar.css does NOT contain `rgb(0, 0, 0)` or `rgb(247,105,0)` or `#f60` or `#e55` or `#94a3b8`
    - TopBar.css contains `background-color: var(--bg-surface)` on `.header`
    - TopBar.css contains `border-bottom: 1px solid var(--border)` on `.header`
    - TopBar.css contains `.topbar-brand` class with `color: var(--accent-blue)`
  </acceptance_criteria>
  <done>TopBar shows "StockSentimentSense" text logo with accent blue styling; Settings dialog has theme toggle with sun/moon icon; all hardcoded hex values replaced with CSS variable references; TopBar background matches surface color with divider border.</done>
</task>

<task type="auto">
  <name>Task 2: CSS variable refactor — SentimentHeatmap, SectorSentimentRow, StockChart; add .surface-card to App.css; migrate .glass-card uses</name>
  <files>
    frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/SentimentHeatmap.css
    frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/SentimentHeatmap.js
    frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/SectorSentimentRow.css
    frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/SectorSentimentRow.js
    frontend/stock_sentiment_analysis/src/components/StockChart/StockChart.css
    frontend/stock_sentiment_analysis/src/App.css
  </files>
  <read_first>
    - frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/SentimentHeatmap.css
    - frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/SentimentHeatmap.js (lines 136-165 — skeleton div and glass-card usage)
    - frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/SectorSentimentRow.css
    - frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/SectorSentimentRow.js (lines 42-60 — Skeleton and error Button sx props)
    - frontend/stock_sentiment_analysis/src/components/StockChart/StockChart.css
    - frontend/stock_sentiment_analysis/src/App.css (current content — .surface-card must be appended)
  </read_first>
  <action>
**App.css — add .surface-card utility** (per D-28, D-29, research anti-pattern note on .glass-card):

Append to `frontend/stock_sentiment_analysis/src/App.css` after the existing `.pct-badge--down` block:
```css
/* Surface card utility — replaces .glass-card globally (D-28, D-29) */
.surface-card {
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 16px;
}
```

**SentimentHeatmap.css — replace hardcoded values** (per VIS-06, research audit):
- `.heatmap-tooltip` background: `#1e293b` → `var(--bg-surface)`
- `.heatmap-tooltip` border: `rgba(255,255,255,0.1)` → `var(--border)`
- `.tooltip-price` color: `#94a3b8` → `var(--text-secondary)`
- `.tooltip-sentiment` color: `#94a3b8` → `var(--text-secondary)`
- `.heatmap-error, .heatmap-empty` color: `#94a3b8` → `var(--text-secondary)`

**SentimentHeatmap.js — two changes** (per D-28, D-30, VIS-09, VIS-10):

1. In the loading skeleton (line ~138), change the inline `style` object:
   - Change `background: 'rgba(255,255,255,0.08)'` to `background: 'var(--bg-elevated)'`

2. In the return JSX (line ~160), change `className="sentiment-heatmap glass-card"` to `className="sentiment-heatmap surface-card"`.
   - NOTE: Do NOT change the `getSentimentColor()` function — those are sentiment palette values locked in Phase 5, not theme colors.

**SectorSentimentRow.css — replace hardcoded values** (per VIS-06, research audit):
- `.sector-count` color: `#94a3b8` → `var(--text-secondary)`
- `.sector-row-error` color: `#94a3b8` → `var(--text-secondary)`

**SectorSentimentRow.js — update sx props** (per D-29, D-30, VIS-07, VIS-09, VIS-10):

1. In the loading skeleton (line ~46), change the `Skeleton` sx prop:
   - Change `bgcolor: 'rgba(255,255,255,0.08)'` → `backgroundColor: 'var(--bg-elevated)'`
   - CRITICAL: Use `backgroundColor` (full property name), NOT `bgcolor` — MUI's `bgcolor` shorthand routes through the palette system and ignores CSS variables. See research Pitfall 2.

2. In the error state Button (line ~56), update sx prop:
   - Change `color: '#94a3b8'` → `color: 'var(--text-secondary)'`
   - Change `borderColor: '#475569'` → `borderColor: 'var(--text-disabled)'`

3. In each rendered `.sector-card` JSX, add `surface-card` to the className (or add inline style for the surface treatment per D-29). The `.sector-card` already has layout styles in SectorSentimentRow.css; add the surface card treatment by changing:
   ```jsx
   <div className="sector-card" ...>
   ```
   to:
   ```jsx
   <div className="sector-card surface-card" ...>
   ```
   NOTE: Do NOT change the `getSentimentColor()` return values in SectorSentimentRow.js — those are sentiment palette values.

**StockChart.css — replace hardcoded values** (per VIS-06, research audit):
- `.ticker-strip` `background-color: #0f172a` → `background-color: var(--bg-page)`
- `.ticker-strip` `border-top: 1px solid rgba(255, 255, 255, 0.1)` → `border-top: 1px solid var(--border)`
  </action>
  <verify>
    <automated>grep -rn "rgba(255,255,255,0.08)\|#94a3b8\|#1e293b\|glass-card\|bgcolor:" frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/ frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/ frontend/stock_sentiment_analysis/src/components/StockChart/StockChart.css</automated>
  </verify>
  <acceptance_criteria>
    - App.css contains `.surface-card` class with `background: var(--bg-surface)` and `border: 1px solid var(--border)` and `border-radius: 8px`
    - SentimentHeatmap.css does NOT contain `#1e293b` (replaced with var(--bg-surface))
    - SentimentHeatmap.css does NOT contain `rgba(255,255,255,0.1)` in `.heatmap-tooltip` (replaced with var(--border))
    - SentimentHeatmap.css does NOT contain `#94a3b8` (all 3 occurrences replaced)
    - SentimentHeatmap.js does NOT contain `className="sentiment-heatmap glass-card"` (replaced with `surface-card`)
    - SentimentHeatmap.js skeleton div does NOT contain `rgba(255,255,255,0.08)` (replaced with `var(--bg-elevated)`)
    - SectorSentimentRow.css does NOT contain `#94a3b8`
    - SectorSentimentRow.js Skeleton does NOT use `bgcolor:` shorthand (uses `backgroundColor:` instead)
    - SectorSentimentRow.js Skeleton uses `backgroundColor: 'var(--bg-elevated)'`
    - SectorSentimentRow.js sector cards have `surface-card` in className
    - StockChart.css does NOT contain `#0f172a` (replaced with var(--bg-page))
    - StockChart.css does NOT contain `rgba(255, 255, 255, 0.1)` (replaced with var(--border))
  </acceptance_criteria>
  <done>.surface-card utility exists in App.css; .glass-card fully replaced in SentimentHeatmap.js and SectorSentimentRow.js; all CSS files use variable references; Skeleton loaders use backgroundColor (not bgcolor) with var(--bg-elevated).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -n "download.png\|glass-card\|rgb(247,105,0)\|rgb(0, 0, 0)" frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.js frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.css` returns NO matches
2. `grep -n "StockSentimentSense" frontend/stock_sentiment_analysis/src/components/TopBar/TopBar.js` returns a match
3. `grep -n "surface-card" frontend/stock_sentiment_analysis/src/App.css frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/SentimentHeatmap.js frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/SectorSentimentRow.js` returns matches in all 3 files
4. `grep -n "bgcolor:" frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/SectorSentimentRow.js` returns NO matches (using backgroundColor instead)
5. `grep -rn "#0f172a\|#1e293b\|#94a3b8\|rgba(255,255,255,0.08)" frontend/stock_sentiment_analysis/src/components/SentimentHeatmap/ frontend/stock_sentiment_analysis/src/components/SectorSentimentRow/ frontend/stock_sentiment_analysis/src/components/StockChart/` returns NO matches
</verification>

<success_criteria>
- TopBar displays "StockSentimentSense" text logo; orange h1 and logo img removed; background is var(--bg-surface) with bottom border
- Settings dialog includes theme toggle (sun/moon icon) below refresh interval control
- All sx props in TopBar.js use CSS variable strings — no hardcoded hex values
- SentimentHeatmap and SectorSentimentRow use .surface-card class (flat, no glassmorphism)
- All 4 CSS files (SentimentHeatmap, SectorSentimentRow, StockChart, TopBar) use var() references
- Skeleton loaders use backgroundColor: 'var(--bg-elevated)' (not bgcolor shorthand)
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-design-overhaul/06-02-SUMMARY.md`
</output>
