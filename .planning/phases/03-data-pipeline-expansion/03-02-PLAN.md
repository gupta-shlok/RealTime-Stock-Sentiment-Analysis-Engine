---
phase: 03-data-pipeline-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - backend/main.py
autonomous: true
gap_closure: false
requirements:
  - "DATA-02"
  - "DATA-03"
  - "DATA-04"
must_haves:
  truths:
    - "GET /stock-price returns data for all 102 tickers organized under GICS sector labels"
    - "Stock history fetched using yf.download() in two batches (first ~50, remaining ~52) with 1.5s delay between; total fetch time under 10 seconds on warm connection"
    - "News fetching uses tiered priority: Tier1 (top 20 by market cap always), Tier2 (next 40 rotating), Tier3 (on-demand for specific ticker)"
    - "News deduplication active: each article UUID appears only once across the aggregated feed"
  artifacts:
    - path: "backend/main.py"
      provides: "Refactored /stock-price with batch download and sector grouping; tiered news with rotation; UUID deduplication"
      contains:
        - "from .tickers import TICKER_DATA, ALL_TICKERS, SECTOR_TICKERS"
        - "TIER2_OFFSET"
        - "/stock-price.*yf\\.download"
        - "asyncio.sleep(1.5)"
        - "/news.*tier"
        - "seen_uuids"
  key_links:
    - from: "backend/main.py:/stock-price"
      to: "backend.tickers:TICKER_DATA"
      via: "import and batch splitting"
      pattern: "from\\.tickers import TICKER_DATA"
    - from: "backend/main.py:/stock-price"
      to: "yf.download"
      pattern: "yf\\.download\\("
    - from: "backend/main.py:/stock-price"
      to: "sector grouping output"
      pattern: "for ticker.*TICKER_DATA"
    - from: "backend/main.py:/news"
      to: "TICKER_DATA.*market_cap"
      pattern: "market_cap"
    - from: "backend/main.py:/news"
      to: "TIER2_OFFSET"
      pattern: "TIER2_OFFSET"
    - from: "backend/main.py:/news"
      to: "seen_uuids"
      pattern: "seen_uuids"
---

<objective>
Refactor data endpoints to scale to all 102 tickers with efficient batch fetching, tiered news rotation, and UUID deduplication.

Purpose: Achieve Phase 3 success criteria: all tickers served, efficient fetch, prioritized news, no duplicates.

Output: Modified backend/main.py with three improvements: batched /stock-price with sector grouping, tiered /news with rotating offset, and UUID deduplication.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-performance/02-01-SUMMARY.md
@.planning/phases/03-data-pipeline-expansion/03-01-SUMMARY.md

# Phase 3: Data Pipeline Expansion
# Requirements: DATA-02, DATA-03, DATA-04
# This plan covers DATA-02, DATA-03, DATA-04
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor /stock-price to use batch yf.download() with sector grouping</name>
  <files>backend/main.py</files>
  <action>
    - Import ticker data: `from .tickers import TICKER_DATA, ALL_TICKERS, SECTOR_TICKERS`
    - Replace the current /stock-price implementation with batch logic:
        1. Retrieve all tickers: `symbols = ALL_TICKERS`
        2. Split into batches of up to 50: `batches = [symbols[i:i+50] for i in range(0, len(symbols), 50)]`. For 102 tickers this yields 3 batches; to adhere to "2 batched calls", combine the last two batches into one larger batch? Actually to minimize requests while honoring "chunks of 50", the intent is to use as few requests as possible. We will use 2 batches: first 50, remaining 52.
           - Implement: if len(symbols) > 50: batches = [symbols[:50], symbols[50:]]
           - This ensures exactly 2 calls for 102 tickers.
        3. Initialize `all_data = {}`
        4. For each batch in batches:
            - Call `yf.download(batch, period='1y', interval='1d', threads=False)` to get DataFrame with MultiIndex columns (ticker, OHLC).
            - Handle potential errors (empty DataFrame) gracefully.
            - For each ticker in the batch, compute:
                - current_close: last Close
                - previous_close: second-to-last Close or same if only one
                - percent_change
                - monthly aggregation: group by month, take first Open, last Close, max High, min Low
            - Store result for the ticker in a flat dict keyed by ticker symbol (as before).
            - If this is not the last batch, `await asyncio.sleep(1.5)` to be polite.
        5. After processing all batches, reorganize data by GICS sector:
            ```
            sector_data = {}
            for ticker, data in all_data.items():
                sector = TICKER_DATA[ticker]['sector']
                sector_data.setdefault(sector, {})[ticker] = data
            ```
        6. Cache `sector_data` in `stock_cache` and return it.
    - Remove old `fetch_ticker_data` and `bounded_fetch` code, along with the `Semaphore`.
    - Keep error logging but ensure a single ticker failure doesn't fail the entire batch.
    - Handle hyphen normalization: ensure ticker keys in TICKER_DATA match the format returned by yf.download (yfinance often normalizes to uppercase with '.' for class shares, e.g., 'BRK.B'). The TICKER_DATA should use the format that works with yf.download. Document this choice; if needed, map TICKER_DATA keys to yfinance format at runtime.
    - No change to response shape (per-ticker data stays same), but now nested under sector keys.
  </action>
  <verify>
    <automated>grep -q "yf.download" backend/main.py</automated>
    <automated>grep -q "await asyncio.sleep(1.5)" backend/main.py</automated>
    <automated>python -c "from backend.main import app; from fastapi.testclient import TestClient; client = TestClient(app); resp = client.get('/stock-price'); assert resp.status_code == 200; data = resp.json(); assert len(data) >= 1; # at least one sector; total_tickers = sum(len(sector_dict) for sector_dict in data.values()); assert total_tickers >= 100"</automated>
    <automated>grep -A 5 "batches =" backend/main.py | grep -q "50"</automated>
  </verify>
  <done>/stock-price returns sector-grouped data for all tickers via two yf.download() calls with 1.5s delay; fetch time <10s on warm connection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement tiered news fetching with rotating Tier 2 offset</name>
  <files>backend/main.py</files>
  <action>
    - At module level, add `TIER2_OFFSET = 0`
    - In the `/news` endpoint, replace the `search_symbols` logic with:
        - If `ticker` query param provided, `search_symbols = [ticker]` (same as before) and do not apply tiering.
        - For the aggregated feed (no ticker):
            1. Build list of (ticker, market_cap) from TICKER_DATA; exclude any with market_cap == 0 or missing.
            2. Sort by market_cap descending: `sorted_tickers = [t for t, _ in sorted(pairs, key=lambda x: x[1], reverse=True)]`
            3. Partition:
               - Tier1: `sorted_tickers[0:20]` (top 20)
               - Tier2 pool: `sorted_tickers[20:60]` (next 40)
               - Tier3: `sorted_tickers[60:]` (remaining, not fetched in aggregation)
            4. Compute the current rotating slice from Tier2 pool. Because TIER2_OFFSET may not be aligned to len(pool)=40, we compute start = TIER2_OFFSET % len(pool) and select 40 tickers wrapping around:
               ```
               pool = tier2_pool
               n = len(pool)
               start = TIER2_OFFSET % n if n > 0 else 0
               tier2 = [pool[(start + i) % n] for i in range(min(40, n))]
               ```
            5. news_fetch_list = Tier1 + tier2
            6. After completing the fetch (before return), update `TIER2_OFFSET = (TIER2_OFFSET + 40) % len(tier2_pool)` (if tier2_pool not empty) to rotate for next call.
        - Log the current tier composition for debugging.
    - Keep the existing per-ticker news fetch loop, replacing `selected_symbols[:5]` with `news_fetch_list`.
    - Ensure the cache key uses the current tier offset so that different rotations are cached separately? Not strictly required since news TTL is short. But we can include offset in cache key if needed. Simpler: don't cache aggregated news differently; the rotation changes the set of tickers, so it's okay to cache what was fetched.
    - No change to the limit of 20 articles after deduplication.
  </action>
  <verify>
    <automated>grep -q "TIER2_OFFSET" backend/main.py</automated>
    <python -c "from backend.main import TIER2_OFFSET; assert isinstance(TIER2_OFFSET, int)"</python>
    <automated>grep -A 30 "def get_news" backend/main.py | grep -q "20:60" or equivalent slice</automated>
    <automated>grep -A 20 "tier2" backend/main.py | grep -q "TIER2_OFFSET"</automated>
  </verify>
  <done>Aggregated news fetches Tier1 always, selects 40 from Tier2 pool with rotation after each call, Tier3 excluded; on-demand ticker query bypasses tiering</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add UUID-based news deduplication across tickers</name>
  <files>backend/main.py</files>
  <action>
    - In the `/news` endpoint, before the loop that appends items to `all_news`, initialize `seen_uuids = set()`.
    - For each `item` in `news_items`, check:
        ```
        uuid = item.get('uuid')
        if uuid and uuid in seen_uuids:
            continue
        if uuid:
            seen_uuids.add(uuid)
        ```
    - If no uuid is present, optionally fallback to a composite key using `item.get('title', '') + str(item.get('providerPublishTime', ''))` to reduce duplicates, but this is best-effort.
    - Deduplication must occur before the sentiment scoring step if possible to avoid wasted compute? Actually in current code, scoring is done before appending. Should move deduplication after fetching news_items but before scoring and appending to `all_news`. Actually current flow: for each item, check premium, then score, then all_news.append(item). We should deduplicate before scoring to avoid unnecessary sentiment calls. So restructure inner loop:
        ```
        for item in news_items:
            if is_premium: continue
            # deduplicate
            if uuid and uuid in seen_uuids: continue
            if uuid: seen_uuids.add(uuid)
            # then compute sentiment and append
            title = item.get('title', "")
            fb_val = analyze_sentiment_ensemble(title)
            ...
            all_news.append(item)
        ```
    - Ensure deduplication is applied across all tickers in the fetch list, so an article that appears for multiple tickers is only scored once and included once.
    - Keep existing sorting and limit to top 20 after deduplication.
  </action>
  <verify>
    <automated>grep -q "seen_uuids" backend/main.py</automated>
    <automated>grep -B 5 -A 10 "for item in news_items" backend/main.py | grep -q "seen_uuids"</automated>
    <automated>python -c "from backend.main import app; from fastapi.testclient import TestClient; client = TestClient(app); # Simulate same article for two tickers; ensure only one appears" (conceptual test)</automated>
  </verify>
  <done>Aggregated news results contain no duplicate UUIDs across tickers; each article appears only once</done>
</task>

</tasks>

<verification>
- All endpoints return expected structures with batch fetch, tiered selection, and deduplication.
- Automated checks pass as defined in each task.
</verification>

<success_criteria>
- DATA-02: /stock-price uses yf.download in two batches (first 50, remaining ~52) with 1.5s delay; total fetch time <10s warm; data organized by GICS sector.
- DATA-03: /news tiering: Tier1 (top 20) always, Tier2 (next 40) rotates via TIER2_OFFSET, Tier3 on-demand.
- DATA-04: /news deduplication by uuid ensures each uuid appears only once in aggregated feed.
- All tickers from TICKER_DATA are served in /stock-price.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-pipeline-expansion/03-02-SUMMARY.md` summarizing the refactor, tiering implementation details, and deduplication approach.
</output>
