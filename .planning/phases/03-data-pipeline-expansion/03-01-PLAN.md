---
phase: 03-data-pipeline-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tickers.py
autonomous: true
gap_closure: false
requirements:
  - "DATA-01"
must_haves:
  truths:
    - "tickers.py contains 102 S&P 100 tickers with GICS sector and market_cap fields"
    - "TICKER_DATA dict is importable and serves as single source of truth for ticker metadata"
  artifacts:
    - path: "backend/tickers.py"
      provides: "ticker to sector and market cap mapping"
      min_lines: 102
    - path: ".planning/phases/03-data-pipeline-expansion/03-01-SUMMARY.md"
      provides: "plan summary"
  key_links:
    - from: "backend/main.py"
      to: "backend.tickers"
      via: "import TICKER_DATA"
      pattern: "from\\.tickers import TICKER_DATA"
---

<objective>
Create tickers.py with the full S&P 100 constituent list (102 tickers) including GICS sector and market cap for each ticker. This becomes the single source of truth for the entire data pipeline.

Purpose: All downstream endpoints need authoritative ticker metadata (sector for grouping, market_cap for tiering, symbol list for iteration).

Output: backend/tickers.py with TICKER_DATA dictionary keyed by ticker symbol.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-performance/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate S&P 100 ticker list and create tickers.py</name>
  <files>backend/tickers.py</files>
  <action>
    - Research and obtain the current S&P 100 constituent list (should total 102 tickers due to dual-class shares like GOOGL/GOOG and hyphenated tickers). Include both GOOGL and GOOG, and BRK-B. Verify against official S&P Dow Jones factsheet as noted in STATE.md.
    - For each ticker, use yfinance (executed synchronously at build time) to fetch Ticker.info and extract:
        - 'sector': the GICS sector (e.g., "Information Technology", "Health Care")
        - 'marketCap': the current market capitalization (in USD)
      If 'sector' is missing, fallback to "Unknown". If 'marketCap' missing, use 0.
    - Create a Python module backend/tickers.py containing:
        TICKER_DATA = {
            'AAPL': {'sector': 'Information Technology', 'market_cap': 3000000000000},
            ...
        }
      with all 102 entries sorted alphabetically by ticker for readability.
    - Also define constants:
        ALL_TICKERS = sorted(TICKER_DATA.keys())
        SECTOR_TICKERS: dict mapping each sector to list of tickers in that sector (precomputed).
    - Ensure the file has no syntax errors (can be imported) and adheres to Python standards.
    - Use consistent ticker keys that match yfinance expectations (e.g., "BRK-B" or "BRK.B"; document choice).
  </action>
  <verify>
    <automated>python -c "from backend.tickers import TICKER_DATA; print(len(TICKER_DATA))" | grep -q 102</automated>
    <automated>python -c "from backend.tickers import TICKER_DATA; assert 'AAPL' in TICKER_DATA; assert 'GOOGL' in TICKER_DATA; assert 'GOOG' in TICKER_DATA; assert 'BRK-B' in TICKER_DATA or 'BRK.B' in TICKER_DATA"</automated>
    <automated>python -c "from backend.tickers import TICKER_DATA, ALL_TICKERS, SECTOR_TICKERS; assert ALL_TICKERS == sorted(TICKER_DATA.keys()); assert isinstance(SECTOR_TICKERS, dict)"</automated>
  </verify>
  <done>backend/tickers.py exists with 102 ticker entries, each with sector and market_cap, and exports TICKER_DATA, ALL_TICKERS, SECTOR_TICKERS</done>
</task>

</tasks>

<verification>
Tickers file can be imported, contains all required fields, and includes both GOOGL and GOOG plus BRK hyphen handling.
</verification>

<success_criteria>
- Ticker count exactly 102 (includes dual-class shares)
- Each entry has 'sector' and 'market_cap'
- GOOGL and GOOG both present
- Hyphen normalization consistent with yfinance (document which form used)
- File imports without error
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-pipeline-expansion/03-01-SUMMARY.md` summarizing the ticker list creation and any assumptions about sector mapping or market cap snapshot.
</output>
